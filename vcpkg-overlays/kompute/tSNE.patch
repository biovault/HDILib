diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4bd75fc..99b42af 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -224,15 +224,15 @@ endif()
 
 # pybind11
 if(KOMPUTE_OPT_BUILD_PYTHON)
-    if(KOMPUTE_OPT_USE_BUILT_IN_PYBIND11)
-        FetchContent_Declare(pybind GIT_REPOSITORY https://github.com/pybind/pybind11.git
-            GIT_TAG v2.9.2
-            GIT_SHALLOW 1) # Source: https://github.com/pybind/pybind11/releases
-        FetchContent_MakeAvailable(pybind)
+    if(KOMPUTE_OPT_USE_BUILT_IN_PYBIND11)   
+            FetchContent_Declare(pybind11 GIT_REPOSITORY https://github.com/pybind/pybind11.git
+                    GIT_TAG v2.9.2
+                    GIT_SHALLOW 1
+                    OVERRIDE_FIND_PACKAGE) # Source: https://github.com/pybind/pybind11/releases
+            FetchContent_MakeAvailable(pybind11)
     else()
-        find_package(pybind11 REQUIRED)
+            find_package(pybind11 REQUIRED)
     endif()
-
     find_package(PythonLibs REQUIRED)
 endif()
 
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 1ad0965..2d892d0 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -95,6 +95,9 @@ if(NOT KOMPUTE_OPT_ANDROID_BUILD AND KOMPUTE_OPT_USE_BUILT_IN_VULKAN_HEADER)
     target_link_libraries(kompute PUBLIC Vulkan-Headers)
 endif()
 
+target_compile_definitions(kompute PRIVATE VULKAN_HPP_DISPATCH_LOADER_DYNAMIC=1)
+
+
 # ####################################################
 # Misc
 # ####################################################
diff --git a/src/include/kompute/Algorithm.hpp b/src/include/kompute/Algorithm.hpp
index c459bab..28d783f 100644
--- a/src/include/kompute/Algorithm.hpp
+++ b/src/include/kompute/Algorithm.hpp
@@ -291,6 +291,11 @@ class Algorithm
 
     void destroy();
 
+    /**
+     * Gets descriptor set used by the algorithm.
+     */
+    std::shared_ptr<vk::DescriptorSet> getDescriptorSet() { return this->mDescriptorSet; }
+
   private:
     // -------------- NEVER OWNED RESOURCES
     std::shared_ptr<vk::Device> mDevice;
diff --git a/src/Algorithm.cpp b/src/Algorithm.cpp
index af12e09..472cb4a 100644
--- a/src/Algorithm.cpp
+++ b/src/Algorithm.cpp
@@ -128,8 +128,9 @@ Algorithm::destroy()
 void
 Algorithm::createParameters()
 {
-    uint32_t numImages = 0;
-    uint32_t numTensors = 0;
+  uint32_t numImages = 0;
+  uint32_t numTensors = 0;
+    uint32_t numUboTensors = 0;
 
     KP_LOG_DEBUG("Kompute Algorithm createParameters started");
 
@@ -137,7 +138,12 @@ Algorithm::createParameters()
         if (mem->type() == Memory::Type::eImage) {
             numImages++;
         } else {
-            numTensors++;
+            auto tensor = std::dynamic_pointer_cast<Tensor>(mem);
+            if (tensor->getDescriptorType() == vk::DescriptorType::eUniformBuffer) {
+              numUboTensors++;
+            } else {
+              numTensors++;
+            }
         }
     }
 
@@ -147,6 +153,13 @@ Algorithm::createParameters()
         descriptorPoolSizes.push_back(vk::DescriptorPoolSize(
           vk::DescriptorType::eStorageBuffer,
           static_cast<uint32_t>(numTensors) // Descriptor count
+        ));
+    }
+
+    if (numUboTensors > 0) {
+        descriptorPoolSizes.push_back(vk::DescriptorPoolSize(
+          vk::DescriptorType::eUniformBuffer,
+          static_cast<uint32_t>(numUboTensors) // Descriptor count
           ));
     }
 
diff --git a/src/include/kompute/Image.hpp b/src/include/kompute/Image.hpp
index d254149..ec11235 100644
--- a/src/include/kompute/Image.hpp
+++ b/src/include/kompute/Image.hpp
@@ -302,6 +302,8 @@ class Image : public Memory
     std::shared_ptr<vk::Image> getPrimaryImage();
     vk::ImageLayout getPrimaryImageLayout();
 
+    void setSampler(vk::Sampler sampler);
+
     /***
      * Retreive the number of channels in the image
      *
@@ -311,6 +313,9 @@ class Image : public Memory
 
     Type type() override { return Type::eImage; }
 
+    void enableSampling() { assert(bool(mSampler)); mSamplingMode = true; }
+    void disableSampling() { mSamplingMode = false; }
+
   protected:
     // -------------- ALWAYS OWNED RESOURCES
     uint32_t mNumChannels;
@@ -319,6 +324,8 @@ class Image : public Memory
     vk::ImageLayout mStagingImageLayout = vk::ImageLayout::eUndefined;
     std::shared_ptr<vk::ImageView> mImageView = nullptr;
     vk::ImageTiling mTiling = vk::ImageTiling::eOptimal;
+    vk::Sampler mSampler;
+    bool mSamplingMode = false;
 
   private:
     // -------------- OPTIONALLY OWNED RESOURCES
diff --git a/src/Image.cpp b/src/Image.cpp
index a9344c3..df675fc 100644
--- a/src/Image.cpp
+++ b/src/Image.cpp
@@ -108,15 +108,15 @@ Image::recordCopyFrom(const vk::CommandBuffer& commandBuffer,
 
     copyFromImage->recordPrimaryImageBarrier(
       commandBuffer,
-      vk::AccessFlagBits::eMemoryRead,
-      vk::AccessFlagBits::eMemoryWrite,
+      vk::AccessFlagBits::eTransferWrite,
+      vk::AccessFlagBits::eTransferRead,
       vk::PipelineStageFlagBits::eTransfer,
       vk::PipelineStageFlagBits::eTransfer,
       vk::ImageLayout::eTransferSrcOptimal);
 
     this->recordPrimaryImageBarrier(commandBuffer,
-                                    vk::AccessFlagBits::eMemoryRead,
-                                    vk::AccessFlagBits::eMemoryWrite,
+                                    vk::AccessFlagBits::eTransferRead,
+                                    vk::AccessFlagBits::eTransferWrite,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::ImageLayout::eTransferDstOptimal);
@@ -174,15 +174,15 @@ Image::recordCopyFromStagingToDevice(const vk::CommandBuffer& commandBuffer)
     KP_LOG_DEBUG("Kompute Image copying size {},{}.", size.width, size.height);
 
     this->recordStagingImageBarrier(commandBuffer,
-                                    vk::AccessFlagBits::eMemoryRead,
-                                    vk::AccessFlagBits::eMemoryWrite,
+                                    vk::AccessFlagBits::eTransferWrite,
+                                    vk::AccessFlagBits::eTransferRead,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::ImageLayout::eTransferSrcOptimal);
 
     this->recordPrimaryImageBarrier(commandBuffer,
-                                    vk::AccessFlagBits::eMemoryRead,
-                                    vk::AccessFlagBits::eMemoryWrite,
+                                    vk::AccessFlagBits::eTransferRead,
+                                    vk::AccessFlagBits::eTransferWrite,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::ImageLayout::eTransferDstOptimal);
@@ -210,15 +210,15 @@ Image::recordCopyFromDeviceToStaging(const vk::CommandBuffer& commandBuffer)
     KP_LOG_DEBUG("Kompute Image copying size {},{}.", size.width, size.height);
 
     this->recordPrimaryImageBarrier(commandBuffer,
-                                    vk::AccessFlagBits::eMemoryRead,
-                                    vk::AccessFlagBits::eMemoryWrite,
+                                    vk::AccessFlagBits::eTransferWrite,
+                                    vk::AccessFlagBits::eTransferRead,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::ImageLayout::eTransferSrcOptimal);
 
     this->recordStagingImageBarrier(commandBuffer,
-                                    vk::AccessFlagBits::eMemoryRead,
-                                    vk::AccessFlagBits::eMemoryWrite,
+                                    vk::AccessFlagBits::eTransferRead,
+                                    vk::AccessFlagBits::eTransferWrite,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::PipelineStageFlagBits::eTransfer,
                                     vk::ImageLayout::eTransferDstOptimal);
@@ -415,7 +415,11 @@ Image::constructDescriptorImageInfo()
     vk::DescriptorImageInfo descriptorInfo;
 
     descriptorInfo.imageView = *(mImageView.get());
-    descriptorInfo.imageLayout = this->mPrimaryImageLayout;
+    if (mSamplingMode) {
+      descriptorInfo.imageLayout = vk::ImageLayout::eShaderReadOnlyOptimal;
+    } else {
+      descriptorInfo.imageLayout = this->mPrimaryImageLayout;
+    }
     return descriptorInfo;
 }
 
@@ -444,11 +448,13 @@ Image::getPrimaryImageUsageFlags()
         case MemoryTypes::eHost:
         case MemoryTypes::eDeviceAndHost:
             return vk::ImageUsageFlagBits::eStorage |
+                   vk::ImageUsageFlagBits::eSampled |
                    vk::ImageUsageFlagBits::eTransferSrc |
                    vk::ImageUsageFlagBits::eTransferDst;
             break;
         case MemoryTypes::eStorage:
             return vk::ImageUsageFlagBits::eStorage |
+                   vk::ImageUsageFlagBits::eSampled |
                    // You can still copy images to/from storage memory
                    // so set the transfer usage flags here.
                    vk::ImageUsageFlagBits::eTransferSrc |
@@ -752,4 +758,9 @@ Image::getFormat()
             return vk::Format::eUndefined;
     }
 }
+
+void Image::setSampler(vk::Sampler sampler)
+{
+    this->mSampler = sampler;
 }
+}
\ No newline at end of file
diff --git a/src/include/kompute/Tensor.hpp b/src/include/kompute/Tensor.hpp
index 78814e3..946a335 100644
--- a/src/include/kompute/Tensor.hpp
+++ b/src/include/kompute/Tensor.hpp
@@ -38,7 +38,8 @@ class Tensor : public Memory
            uint32_t elementTotalCount,
            uint32_t elementMemorySize,
            const DataTypes& dataType,
-           const MemoryTypes& tensorType = MemoryTypes::eDevice);
+           const MemoryTypes& tensorType = MemoryTypes::eDevice,
+           vk::DescriptorType descriptorType = vk::DescriptorType::eStorageBuffer);
 
     /**
      *  Constructor with size provided which would be used to create the
@@ -176,6 +177,8 @@ class Tensor : public Memory
 
     Type type() override { return Type::eTensor; }
 
+    vk::DescriptorBufferInfo constructDescriptorBufferInfo();
+
   protected:
     // -------------- ALWAYS OWNED RESOURCES
     vk::DescriptorBufferInfo mDescriptorBufferInfo;
@@ -215,8 +218,6 @@ class Tensor : public Memory
     vk::BufferUsageFlags getPrimaryBufferUsageFlags();
     vk::BufferUsageFlags getStagingBufferUsageFlags();
 
-    vk::DescriptorBufferInfo constructDescriptorBufferInfo();
-
     /**
      * Function to reserve memory on the tensor. This does not copy any data, it
      * just reserves memory, similarly to std::vector reserve() method.
@@ -225,8 +226,7 @@ class Tensor : public Memory
 };
 
 template<typename T>
-class TensorT : public Tensor
-{
+class TensorT : public Tensor {
   public:
     TensorT(std::shared_ptr<vk::PhysicalDevice> physicalDevice,
             std::shared_ptr<vk::Device> device,
@@ -246,14 +246,16 @@ class TensorT : public Tensor
       std::shared_ptr<vk::PhysicalDevice> physicalDevice,
       std::shared_ptr<vk::Device> device,
       const std::vector<T>& data,
-      const Memory::MemoryTypes& tensorType = Memory::MemoryTypes::eDevice)
+      const Memory::MemoryTypes& tensorType = Memory::MemoryTypes::eDevice,
+      vk::DescriptorType descriptorType = vk::DescriptorType::eStorageBuffer)
       : Tensor(physicalDevice,
                device,
                (void*)data.data(),
                static_cast<uint32_t>(data.size()),
                sizeof(T),
                Memory::dataType<T>(),
-               tensorType)
+               tensorType,
+               descriptorType)
     {
         KP_LOG_DEBUG("Kompute TensorT filling constructor with data size {}",
                      data.size());
diff --git a/src/Tensor.cpp b/src/Tensor.cpp
index a1eaccf..3df27e5 100644
--- a/src/Tensor.cpp
+++ b/src/Tensor.cpp
@@ -11,7 +11,8 @@ Tensor::Tensor(std::shared_ptr<vk::PhysicalDevice> physicalDevice,
                uint32_t elementTotalCount,
                uint32_t elementMemorySize,
                const DataTypes& dataType,
-               const MemoryTypes& memoryType)
+               const MemoryTypes& memoryType,
+               vk::DescriptorType descriptorType)
   : Memory(physicalDevice, device, dataType, memoryType, elementTotalCount, 1)
 {
     this->mSize = elementTotalCount;
@@ -23,7 +24,7 @@ Tensor::Tensor(std::shared_ptr<vk::PhysicalDevice> physicalDevice,
                  elementTotalCount,
                  Memory::toString(memoryType));
 
-    this->mDescriptorType = vk::DescriptorType::eStorageBuffer;
+    this->mDescriptorType = descriptorType;
 
     this->reserve();
     this->updateRawData(data);
@@ -275,7 +276,7 @@ Tensor::constructDescriptorSet(vk::DescriptorSet descriptorSet,
       binding, // Destination binding
       0,       // Destination array element
       1,       // Descriptor count
-      vk::DescriptorType::eStorageBuffer,
+      this->mDescriptorType,
       nullptr, // Descriptor image info
       // Using a member variable here means the address is still valid later
       &mDescriptorBufferInfo);
@@ -286,25 +287,34 @@ Tensor::constructDescriptorSet(vk::DescriptorSet descriptorSet,
 vk::BufferUsageFlags
 Tensor::getPrimaryBufferUsageFlags()
 {
+    vk::BufferUsageFlags usage;
     switch (this->mMemoryType) {
         case MemoryTypes::eDevice:
         case MemoryTypes::eHost:
         case MemoryTypes::eDeviceAndHost:
-            return vk::BufferUsageFlagBits::eStorageBuffer |
-                   vk::BufferUsageFlagBits::eTransferSrc |
-                   vk::BufferUsageFlagBits::eTransferDst;
+            usage =
+              vk::BufferUsageFlagBits::eTransferSrc |
+              vk::BufferUsageFlagBits::eTransferDst |
+              vk::BufferUsageFlagBits::eIndirectBuffer;
             break;
         case MemoryTypes::eStorage:
-            return vk::BufferUsageFlagBits::eStorageBuffer |
-                   // You can still copy buffers to/from storage memory
-                   // so set the transfer usage flags here.
-                   vk::BufferUsageFlagBits::eTransferSrc |
-                   vk::BufferUsageFlagBits::eTransferDst;
+            usage =
+              // You can still copy buffers to/from storage memory
+              // so set the transfer usage flags here.
+              vk::BufferUsageFlagBits::eTransferSrc |
+              vk::BufferUsageFlagBits::eTransferDst |
+              vk::BufferUsageFlagBits::eIndirectBuffer;
             break;
         default:
-            throw std::runtime_error("Kompute Tensor invalid tensor type");
-    }
-}
+          throw std::runtime_error("Kompute Tensor invalid tensor type");
+      }
+      if (this->mDescriptorType == vk::DescriptorType::eUniformBuffer) {
+        usage |= vk::BufferUsageFlagBits::eUniformBuffer;
+      } else {
+        usage |= vk::BufferUsageFlagBits::eStorageBuffer;
+      }
+      return usage;
+  }
 
 vk::BufferUsageFlags
 Tensor::getStagingBufferUsageFlags()
diff --git a/src/include/kompute/Manager.hpp b/src/include/kompute/Manager.hpp
index 5093c83..8f4e4d4 100644
--- a/src/include/kompute/Manager.hpp
+++ b/src/include/kompute/Manager.hpp
@@ -1,6 +1,7 @@
 // SPDX-License-Identifier: Apache-2.0
 #pragma once
 
+
 #include "kompute/Core.hpp"
 
 #include "kompute/Image.hpp"
@@ -127,6 +128,31 @@ class Manager
 
         return tensor;
     }
+
+    /**
+     * Create a managed ubo tensor that will be destroyed by this manager
+     * if it hasn't been destroyed by its reference count going to zero.
+     *
+     * @param data The data to initialize the tensor with
+     * @param tensorType The type of tensor to initialize
+     * @returns Shared pointer with initialised tensor
+     */
+    template<typename T>
+    std::shared_ptr<TensorT<T>> uboTensorT(
+      const std::vector<T>& data,
+      Memory::MemoryTypes tensorType = Memory::MemoryTypes::eDevice) {
+      KP_LOG_DEBUG("Kompute Manager tensor creation triggered");
+
+      std::shared_ptr<TensorT<T>> tensor{ new kp::TensorT<T>(
+        this->mPhysicalDevice, this->mDevice, data, tensorType, vk::DescriptorType::eUniformBuffer) };
+
+      if (this->mManageResources) {
+        this->mManagedMemObjects.push_back(tensor);
+      }
+
+      return tensor;
+    }
+
     std::shared_ptr<TensorT<float>> tensor(
       const std::vector<float>& data,
       Memory::MemoryTypes tensorType = Memory::MemoryTypes::eDevice)
@@ -429,6 +455,18 @@ class Manager
         return image;
     }
 
+    vk::Sampler createLinearSampler() {
+      vk::SamplerCreateInfo info{};
+      info.magFilter = vk::Filter::eLinear;
+      info.minFilter = vk::Filter::eLinear;
+      info.mipmapMode = vk::SamplerMipmapMode::eLinear;
+      info.addressModeU = vk::SamplerAddressMode::eClampToEdge;
+      info.addressModeV = vk::SamplerAddressMode::eClampToEdge;
+      info.addressModeW = vk::SamplerAddressMode::eClampToEdge;
+      info.unnormalizedCoordinates = VK_FALSE;
+      return this->mDevice->createSampler(info);
+  }
+
     /**
      * Default non-template function that can be used to create algorithm
      * objects which provides default types to the push and spec constants as
@@ -530,6 +568,17 @@ class Manager
      **/
     std::shared_ptr<vk::Instance> getVkInstance() const;
 
+    /**
+     * Get the logical device
+     * */
+    std::shared_ptr<vk::Device> getDevice() {
+      return this->mDevice;
+    }
+
+    std::shared_ptr<vk::PhysicalDevice> getPhysicalDevice() {
+      return this->mPhysicalDevice;
+    }
+
   private:
     // -------------- OPTIONALLY OWNED RESOURCES
     std::shared_ptr<vk::Instance> mInstance = nullptr;
diff --git a/src/Manager.cpp b/src/Manager.cpp
index b74dc59..a34e37a 100644
--- a/src/Manager.cpp
+++ b/src/Manager.cpp
@@ -14,7 +14,6 @@
 #include <sstream>
 #include <string>
 #include <unordered_map>
-
 namespace kp {
 
 #ifndef KOMPUTE_DISABLE_VK_DEBUG_LAYERS
@@ -61,8 +60,10 @@ Manager::Manager(uint32_t physicalDeviceIndex,
 #endif
 
     this->createInstance();
+    VULKAN_HPP_DEFAULT_DISPATCHER.init(*this->mInstance.get());
     this->createDevice(
       familyQueueIndices, physicalDeviceIndex, desiredExtensions);
+    VULKAN_HPP_DEFAULT_DISPATCHER.init(*this->mDevice.get());
 }
 
 Manager::Manager(std::shared_ptr<vk::Instance> instance,
@@ -170,6 +171,13 @@ Manager::createInstance()
 
     this->mFreeInstance = true;
 
+    // initialization to support dynamic dispatcher
+    static bool loaderInitialized = false;
+    if (!loaderInitialized) {
+        VULKAN_HPP_DEFAULT_DISPATCHER.init(vkGetInstanceProcAddr);
+        loaderInitialized = true;
+    }
+
     vk::ApplicationInfo applicationInfo;
     applicationInfo.pApplicationName = "Kompute";
     applicationInfo.pEngineName = "Kompute";
diff --git a/src/include/kompute/Memory.hpp b/src/include/kompute/Memory.hpp
index eb93e57..5c061f4 100644
--- a/src/include/kompute/Memory.hpp
+++ b/src/include/kompute/Memory.hpp
@@ -226,6 +226,13 @@ class Memory
 
     vk::DescriptorType getDescriptorType() { return mDescriptorType; }
 
+    /** Sets the descriptor type for this memory object.
+     * @param newType The new descriptor type to set.
+     *
+     * This is use to override the default Tensor type to support UNIFORM buffers (ubos)
+    */
+    void setDescriptorType(vk::DescriptorType newType) { mDescriptorType = newType; }
+
     /**
      * Retrieve the raw data via the pointer to the memory that contains the raw
      * memory of this current tensor. This tensor gets changed to a nullptr when
diff --git a/src/include/kompute/Sequence.hpp b/src/include/kompute/Sequence.hpp
index ba0e90e..e8c83e1 100644
--- a/src/include/kompute/Sequence.hpp
+++ b/src/include/kompute/Sequence.hpp
@@ -276,7 +276,11 @@ class Sequence : public std::enable_shared_from_this<Sequence>
      */
     void destroy();
 
-  private:
+    /**
+     * Getter for the command buffer used by the sequence for the creation of memory barriers
+     */
+    std::shared_ptr<vk::CommandBuffer> getCommandBuffer() const { return mCommandBuffer; };
+private:
     // -------------- NEVER OWNED RESOURCES
     std::shared_ptr<vk::PhysicalDevice> mPhysicalDevice = nullptr;
     std::shared_ptr<vk::Device> mDevice = nullptr;
